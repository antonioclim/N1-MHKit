# Optional external adapter

The folder contains an optional adapter scaffold for mapping a public wearable-data file into a long-format table. Public datasets are not redistributed in this repository. To use the adapter, download the external dataset independently and run the script locally.
