# Raw data provenance

The raw-data archive keeps the original session-archive structure used by the reproduction scripts. For public release, non-scientific document metadata was removed from spreadsheet and ECG PDF files, generated image metadata was removed from figures, and system files were excluded. Measurement values, filenames required for session matching, CSV tables, WAV audio content and PDF page content were otherwise preserved.
